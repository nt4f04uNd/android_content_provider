name: android_content_provider_platform_interface
description: A common platform interface for the android_content_provider plugin.
version: 0.0.1
homepage: https://github.com/nt4f04uNd/android_content_provider/tree/master/android_content_provider_platform_interface

environment:
  sdk: ">=2.15.0-233.0.dev <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

